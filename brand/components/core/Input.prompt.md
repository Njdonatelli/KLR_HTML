Labeled text field or textarea, used on consultation-request and contact forms.

```jsx
<Input label="What are you envisioning?" placeholder="Tell us about the space" multiline required />
```

Set `multiline` for a textarea (notes, project descriptions). Focus state switches the border to navy.
